{
  "name": "@hana/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared chain config, generated ABIs/addresses, and types for Hana CTC.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "@types/node": "^22.10.2"
  }
}
