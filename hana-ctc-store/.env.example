# Package-local overrides.

MERCHANT_API_URL=http://localhost:3002
# This store's own merchant account (server-side only — never sent to the browser). Register one
# via POST /api/merchants/register on the Merchant API, or through its portal at /register.
MERCHANT_CLIENT_ID=
MERCHANT_CLIENT_SECRET=
